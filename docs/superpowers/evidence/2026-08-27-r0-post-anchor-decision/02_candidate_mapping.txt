R0 — CANDIDATE MAPPING AGAINST do-not-repeat #1-#52
==============================================================================
authority: docs/alphazero-value-search-experiment-ledger.md, Status: CLOSED 2026-08-12
NOTE: the list runs to #52, not #51 -- the twixtbot anchor rejection added
an entry on 2026-08-24, after the closure the authorization cites.

A. LONGER-HORIZON TRAINING
------------------------------------------------------------------------------
  named mechanism       more self-play iterations than 5 from calib020_0001
  ledger status         CLOSED BY NAME. #48: 'extend the same run to 10 or 20 iterations'. #49: 'Do not run a
                        warmup-size grid, extend to ten or twenty iterations, change the dose or learning
                        rate'. #51: 'Do not change the dose or warmup size ... extend beyond five iterations'.
  is horizon a new mechanism?NO. Horizon is a DOSE of an existing mechanism. #49 closes ordinary continuation 'in
                        ANY dose, warm buffer included'; #51 closes frozen-parent opposition 'in any dose or
                        shape'. A longer run is the same family with a larger number.
  cheap falsifier       none available that is not itself the experiment: horizon effects cannot be falsified
                        short of running the horizon.
  evidence to date      cont5 -0.186, warm5 -0.068, fp6 -0.034 vs parent. Three mechanisms, three NEGATIVE
                        movements, none positive.
  VERDICT               REJECT - repeats a closed mechanism (#48, #49, #51).

B. ARCHITECTURE / CAPACITY CHANGE
------------------------------------------------------------------------------
  named mechanism       trunk width above 128 at fixed depth 6
  ledger status         NOT closed by name for the trunk. #29/#31/#34 close width sweeps in the v14 ADAPTER
                        context, a different component. #47 closes progressive widening, which is a SEARCH
                        change, not architecture.
  cheap falsifier       NONE QUALIFIED. C0's review STRUCK width 96 as a positive control: fewer parameters
                        guarantee lower NOMINAL capacity, not worse held-out loss. WITHDRAWN CLAIM: an earlier
                        draft said C0 'already ran' this falsifier and found L(96) ~ L(128). C0 TRAINED NOTHING
                        -- its scope line reads 'read-only. Nothing was trained, loaded, inferred or generated'
                        -- and L(96) ~ L(128) appears there only inside conditionals. No capacity curve has
                        ever been measured in this stack.
  corpus                NO TRAINING CORPUS WITH POLICY TARGETS EXISTS. All 44,900 extant replay games lack pi,
                        and GameSaver cannot write it; pi survives only in 96 retired atlas legs, which cannot
                        serve as a corpus. This is ONE engineering prerequisite among the four blockers here,
                        not the single blocker. WITHDRAWN: an earlier draft said needing new self-play data
                        're-enters #49'. It does not -- #49 closes ORDINARY CONTINUATION FROM THE INCUMBENT,
                        not from-scratch training of a different architecture.
  initialization        NO QUALIFIED WIDENING OR INITIALIZATION METHOD IS SPECIFIED, and the 2026-08-22
                        benchmark reset requires a new card to justify training from scratch. WITHDRAWN: an
                        earlier draft said a width change CANNOT start from calib020_0001's weights. That is
                        stronger than the evidence -- what is supported is that no method for doing so has been
                        specified or qualified here.
  cost                  C0 CANNOT PRICE THIS CANDIDATE. Its ~4.45 GPU-hours per 1,000 steps figure prices ONLY
                        the 128x6 final three-seed ladder, and EXCLUDES the LR grid and checkpoint selection,
                        which C0's own review found are themselves training. For a wider network the
                        throughput, the LR selection, the checkpoint selection and the total step count are ALL
                        UNMEASURED -- calibrating them is what C1 would have been for. WITHDRAWN: an earlier
                        draft said a wider net 'exceeds that, unbounded above'. That is a projection, not a
                        measurement, and C0 supports neither the direction nor the magnitude.
  VERDICT               REJECT - no qualified cheap falsifier, no usable pi corpus, invalid cost estimate,
                        unclosed confounds. NOT rejected on an observed flat curve.

C. TRULY INDEPENDENT TRAINING DATA
------------------------------------------------------------------------------
  named mechanism       training on games not generated by this stack's self-play
  ledger status         not closed by name - no entry contemplates it.
  T1j                   EXCLUDED BY CONSTRUCTION. Training on the anchor converts it into a component of the
                        thing it measures, irreversibly, with no second uncontaminated anchor to fall back on.
  twixtbot              EXCLUDED ON RULES PROVENANCE, NOT ON #52. CORRECTION: #52 closes retrying twixtbot AS
                        AN ANCHOR; it says nothing about training data, and training on twixtbot would NOT
                        contaminate T1j. The valid objection is that E0 recorded it as TRAINED WITH CROSSINGS
                        ALLOWED while our rules have own-link crossing OFF -- its move distribution encodes a
                        different game.
  any third source?     would require a download, which R0 excludes and which no card authorizes. None exists
                        in the repository: the five replay corpora are all self-play, content-pinned but
                        PROVENANCE-LIMITED - no game names its generating network.
  cheap falsifier       cannot be specified without naming a source.
  VERDICT               REJECT - no QUALIFIED uncontaminated source is available WITHIN R0's authorized
                        repository-only scope. That is a scope-bounded negative, not proof that none exists.

==============================================================================
GATE RESULT: NO_GO. No candidate survives, EACH ON ITS OWN MERITS.
The Hoeffding table prices future precision; it rejects nothing.
calib020_0001 remains the incumbent. The research programme stays closed.
