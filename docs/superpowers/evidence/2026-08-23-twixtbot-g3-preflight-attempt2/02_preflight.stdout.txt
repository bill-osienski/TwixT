combined runtime (one process):
  PASS  TensorFlow, MLX and both engines import together — tensorflow 2.21.0, mlx 0.32.1, numpy 2.5.2
  PASS  anchor (TensorFlow) fixed-position evaluation — pwin[1, 3] movelogits[1, 528] -> value +0.5542
  PASS  reference (MLX) fixed-position evaluation, both checkpoints — 2 checkpoints evaluated in-process: {'0379': -0.0497, 'calib020_0001': -0.1002}

pinned artifact hashes:
  PASS  anchor model artifacts match pinned sha256 — 3 anchor artifacts match their pins
  PASS  reference checkpoints match pinned sha1 — both reference checkpoints match their pinned SHA-1

frozen openings:
  PASS  openings replay legally and bind in both engines — 8 openings replay legally and bind identically at every ply
  PASS  openings are colour-arm neutral — each opening is byte-identical across both colour arms

schedule dry run (128 task identities, no game played):
  PASS  schedule invariants hold — 128 tasks, 0 invariant violations
  PASS  seed formula matches the frozen expression — 128 seeds = [202611000, 202611128) exactly; 272 reserved seeds remain unused
  PASS  task counts are balanced — 32 per trials, 16 per trials/reference, 2 per opening, 64 per colour arm

execution paths:
  PASS  trials=0 raw-policy path yields a move — trials=0 move (10, 13); visits unavailable, full 528 policy array preserved
  PASS  trials>0 yields a complete visit array — trials=100 move (9, 15); full 528 visit array, 3 non-zero
  PASS  one Player per GAME, root retained — one Player served two moves ((9, 15), (15, 13)); NeuralMCTS root retained between them
  PASS  reference agent binds seed/checkpoint/settings — seed 202611000 bound; both RNG streams match eval_runner's masks; 400 sims / batch 14 / flush 48 / opening_temperature; out-of-turn refused
  PASS  reference agent plays one real move — reference chose (9, 12) in 1.28s at 400 sims
  PASS  unexpected exceptions are structured aborts — state-update exception recorded as engine_exception; unbound reference refused
  PASS  runner stops on the first aborted task — stops at the first aborted task, leaving the rest unplayed

harness contract:
  PASS  harness passes ply_cap explicitly — play_game passes ply_cap explicitly; parameter keyword-only with no default
  PASS  harness preserves the FULL visit array — full visit array stored per move, no head slice

scope:
  PASS  no G3 game played, no reserved seed consumed — no scheduled task was run and no reserved seed consumed; the 400 seeds in [202611000, 202611400) are untouched. play_game was invoked TWICE, both on the SAME synthetic task (seed -1) with fake engines, to observe abort behaviour; single real moves were played by each engine, but no game.

20/20 checks passed
elapsed 10.91s, max RSS 1537.77 MiB

G3 PREFLIGHT PASSED
