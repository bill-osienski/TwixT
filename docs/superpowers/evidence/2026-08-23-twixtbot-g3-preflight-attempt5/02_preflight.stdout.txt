combined runtime (one process):
  PASS  TensorFlow, MLX and both engines import together — tensorflow 2.21.0, mlx 0.32.1, numpy 2.5.2
  PASS  anchor (TensorFlow) fixed-position evaluation — pwin[1, 3] movelogits[1, 528] -> value +0.5542
  PASS  reference (MLX) fixed-position evaluation, both checkpoints — 2 checkpoints evaluated in-process: {'0379': -0.0497, 'calib020_0001': -0.1002}

pinned artifact hashes:
  PASS  anchor model artifacts match pinned sha256 — 3 anchor artifacts match their pins
  PASS  reference checkpoints match pinned sha1 — both reference checkpoints match their pinned SHA-1

frozen openings:
  PASS  openings replay legally and bind in both engines — 8 openings replay legally and bind identically at every ply
  PASS  openings are colour-arm neutral — each opening is byte-identical across both colour arms

schedule dry run (128 task identities, no game played):
  PASS  schedule invariants hold — 128 tasks, 0 invariant violations
  PASS  seed formula matches the frozen expression — 128 seeds = [202611128, 202611256) exactly, disjoint from 1 consumed; 271 reserved seeds remain unused
  PASS  task counts are balanced — 32 per trials, 16 per trials/reference, 2 per opening, 64 per colour arm

execution paths:
  PASS  trials=0 raw-policy path yields a move — trials=0 move (10, 13); visits unavailable, full 528 policy array preserved
  PASS  trials>0 yields a complete visit array — trials=100 move (9, 15); full 528 visit array, 3 non-zero
  PASS  one Player per GAME, root retained — one Player served two moves ((9, 15), (15, 13)); NeuralMCTS root retained between them
  PASS  preflight seed is outside the reservation — preflight seed 999000001 is outside [202611000, 202611400) and unscheduled
  PASS  reference agent binds seed/checkpoint/settings — synthetic seed bound; both RNG streams match eval_runner's masks; compile=True; 400/14/48/opening_temperature; out-of-turn refused
  PASS  negative bindings are all refused — 6 negative bindings all refused (checkpoint, untagged, config, colour, consumed seed, anchor settings)
  PASS  reference agent plays one real move — reference chose (8, 12) in 1.14s at 400 sims
  PASS  unexpected exceptions are structured aborts — state-update exception recorded as engine_exception; unbound reference refused
  PASS  production entry point has no switches — run_g3 takes only clone_root/repo_root/results_path; persistence mandatory and new
  PASS  anchor runtime identity is verified — clone verified at 83749f2, 3 model artifacts hashed; non-checkout and wrong-commit refused; module origins and cwd bound
  PASS  one shared anchor evaluator — one shared anchor evaluator is passed to every Player; frozen-key check unaffected
PRECONDITION REFUSED: not a git checkout: /nonexistent
  PASS  gate is durable and exit-bound — verdict persisted as a terminal record; PASSED/FAILED/ABORTED map to exit 0/1/2; a refused precondition exits 3
  PASS  counts are games, not records — one game played, two tasks remaining; the run-stop record is not counted as a game
  PASS  summarise rejects partial/duplicate/malformed sets — partial, 127-game, duplicate, alien-reference, malformed and aborted sets all refused; a complete 128 set scores, lowest passing = 100
  PASS  cleanup failure is a run-level abort — cleanup failure stops the run, is persisted durably, and blocks the summary
  PASS  binding failure is a structured, persisted abort — a binding failure is recorded, persisted, and stops the run without a summary
  PASS  runner stops on the first aborted task — stops at the first aborted task, leaving the rest unplayed

harness contract:
  PASS  harness passes ply_cap explicitly — play_game passes ply_cap explicitly; parameter keyword-only with no default
  PASS  harness preserves the FULL visit array — full visit array stored per move, no head slice

scope:
  PASS  no G3 game played, no reserved seed consumed — no scheduled seed was driven or even seeded an RNG; the only seed driven through an engine was 999000001 (outside the reservation). 202611300 is reserved-but-unscheduled and only had a random.Random constructed from it, never drawn. Scheduled task dicts appear as inert DATA tests (no engine, no RNG, no move). 202611000 stays recorded as consumed by attempt 2; the schedule sits on [202611128, 202611256).

30/30 checks passed
elapsed 11.31s, max RSS 1578.39 MiB

G3 PREFLIGHT PASSED
