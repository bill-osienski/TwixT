# Core training dependencies (MLX-based)
mlx>=0.20.0
safetensors>=0.4.0
numpy>=1.24.0

# ONNX export (for Node.js server)
torch>=2.0.0
onnx>=1.15.0
onnxscript>=0.1.0

# Game logic and utilities
# (no external deps - pure Python)

# Optional: development tools
# pip install -r requirements-dev.txt
